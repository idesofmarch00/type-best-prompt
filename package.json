{
  "name": "typing-competition-platform",
  "private": true,
  "version": "1.0.0",
  "description": "AI-powered typing speed and prompt engineering competition platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:db\" \"npm run dev:web\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:db": "docker-compose up -d",
    "build": "cd apps/web && npm run build",
    "test": "cd apps/web && npm test",
    "db:migrate": "cd packages/database && npx prisma migrate dev",
    "db:studio": "cd packages/database && npx prisma studio",
    "setup": "npm install && npm run dev:db && npm run db:migrate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
